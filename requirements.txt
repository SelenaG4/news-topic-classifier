fastapi==0.115.0
uvicorn[standard]==0.30.6
pandas==2.3.3
scikit-learn==1.8.0
pytest==8.3.3
httpx==0.28.1

# This file is the LIGHT, always-needed runtime: the classical TF-IDF/logistic-
# regression baseline, the FastAPI service, and the test suite. It is
# deliberately kept free of torch/transformers so the service builds fast and
# runs in a small footprint (e.g. a free-tier host) -- the app boots and serves
# the baseline with no transformer present, reporting transformer_loaded: false
# honestly (see app/transformer_classifier.py).
#
# To actually run the fine-tuned DistilBERT transformer locally, ALSO install
# the heavier deps:  pip install -r requirements-transformer.txt
# (see README "Running it" and "Experiment tracking").
