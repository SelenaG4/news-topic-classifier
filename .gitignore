__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
*.egg-info/
.DS_Store
.ipynb_checkpoints/

# Fine-tuned weights are large binary files produced by the Colab notebook,
# not meant to live in git -- copy them locally into models/ after running
# the notebook, they'll never get committed.
models/*/
!models/.gitkeep
